import { types } from "../types"

const initialState = {
    currentLang: 'ES',
    availableLang: ['US','FR', 'ES', 'IT']
}

export const langReducer = ( state= initialState,action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}
